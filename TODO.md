- vérifier la coloration syntaxique de mes sql queries.
- vérifier les menus en table de matières
- vérifier le responsive complet

ENUM pas en bleu dans CREATE TABLE (yellow)
Réduction des couleurs (string et integer donc valeur de la meme couleur)

Attention : l'exécution d'une commande DROP est, ,[object Object], ,! Soyez vigilant !
enlever encadré des sql queries dans certaines conditions

ENUM('en_cours', 'rendu', 'en_retard') brown coouleurs
DEFAULT 'en coours' couleur


Footer sur practice

Google analytics ?

MAJ README





Responsive (iphone5):
- ceinture blanche :
    . La phrase "Exemples de SGBD (liste non exhaustive) :" -> les deux points sont "seuls" sur une deuxième ligne. Comment éviter cela ?

    . Dans la rubrique "Contraintes et options" -> Dans les cards, le contenu sous le titre doit être aligner à gauche (au même niveau que l'icône près du titre) + vérifier le centrage vertical entre le titre et l'icône, il semble y avoir un décalage.

    . Les icônes de : types de données courantes, types de données moins courantes, contraintes et options ainsi que exxemple d'une table utilisateur sont très petits. Comment éviter un redimensionnement trop important de ces icônes.

    . Idem pour les icônes dans bonnes pratiques (notamment quand le titre est sur plusieurs lignes).

    .Idem poour les icônes sur les titres des examples (toutes ceintures)
    
- ceintures marron : 
    . Les diagram se superposent. Passer à un par ligne.
    . centrer gauche la légende.

Voir break-word
Icone swip ?

Le clic sur NoobSQL en version mobile ne renvoie pas à l'accueil.
